---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: una-config
  namespace: bootstrap
  labels:
    app: unifi-network-application
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: rook-ceph-retain
