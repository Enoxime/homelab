apiVersion: apps/v1
kind: Deployment

metadata:
  name: yarr-prowlarr
  namespace: yarr
  labels:
    app: prowlarr

spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: prowlarr
  template:
    metadata:
      labels:
        app: prowlarr
    spec:
      securityContext:
        fsGroup: ${groupId}
      containers:
        - name: prowlarr
          image: lscr.io/linuxserver/prowlarr:2.3.5.5327-ls142
          imagePullPolicy: Always
          securityContext:
            capabilities:
              drop: [ "ALL" ]
            allowPrivilegeEscalation: true
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
            readOnlyRootFilesystem: false
            privileged: true
            runAsUser: ${userId}
            runAsGroup: ${groupId}
          ports:
            - containerPort: 9696
              name: http
              protocol: TCP
          envFrom:
            - configMapRef:
                name: prowlarr-env
          volumeMounts:
            - name: config
              mountPath: /config
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: yarr-prowlarr-config
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prowlarr-env
  namespace: yarr
data:
  TZ: ${yarr_timezone}
---
apiVersion: v1
kind: Service
metadata:
  name: yarr-prowlarr
  namespace: yarr
spec:
  ports:
    - protocol: TCP
      port: 9696
      targetPort: http
  selector:
    app: prowlarr
  type: ClusterIP
