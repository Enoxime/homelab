{% for upstream in upstreams %}

upstream {{ upstream.name }} {
{%  for addr in upstream.addresses %}
  server {% if addr.proto is defined %}{{ addr.proto }}://{% endif %}{{ addr.address }}{% if addr.port is defined %}:{{ addr.port }}{% endif %};
{%  endfor %}
}
{% endfor %}