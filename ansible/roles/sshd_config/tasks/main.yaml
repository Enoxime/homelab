---
- name: "Set secured SSHD configuration"
  ansible.builtin.template:
    dest: "/etc/ssh/sshd_config"
    src: "sshd_config"
    owner: "root"
    group: "root"
    mode: "0644"
  notify: "Restart sshd"
...