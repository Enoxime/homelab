{% if filter.description is defined %}# {{ filter.description }}{% endif %}

[Definition]
{% if filter.options is defined %}
    {% for name, value in filter.options.items() %}

{{ name }} = {{ value }}
    {% endfor %}
{% endif %}

failregex = {{ filter.failregex }}

ignoreregex = {% if filter.ignoreregex is defined %}{{ filter.ignoreregex }}{% endif %}
